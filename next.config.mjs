/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
    domains: ['defence.in','images.unsplash.com', "plus.unsplash.com","edasglobalsupplychain.com" ], // 👈 Add the domain here
  },
};

export default nextConfig;
